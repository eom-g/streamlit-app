name: streamlit-app
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - streamlit
    - pandas
    - numpy
    - plotly
    - google-generativeai
    - optbinning
    - sweetviz
    - ydata-profiling
    - setuptools  # 이 줄을 추가하세요!
